{% docs software_client %}
The name of the Solana client software implementation that the validator is running. Common values include "Agave" (the primary Solana client), "Firedancer" (Jump Crypto's high-performance client), and others. This field helps track client diversity across the network, which is important for network resilience and decentralization.

Data type: STRING
{% enddocs %}
