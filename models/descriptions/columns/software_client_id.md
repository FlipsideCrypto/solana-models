{% docs software_client_id %}
Numeric identifier corresponding to the software client implementation that the validator is running. This provides a standardized way to identify and categorize different Solana client implementations. The ID maps to specific client software like Agave, Firedancer, etc., and is used for tracking client distribution and network diversity metrics.

Data type: STRING (numeric identifier)
{% enddocs %}
