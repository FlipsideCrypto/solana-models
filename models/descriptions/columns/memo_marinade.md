{% docs memo_marinade %}

Raw memo text in JSON included by the client or protocol for the transaction. Contains micro-metadata such as user identifiers, action tags, and optional parameters used by Marinade’s off-chain systems.

{% enddocs %}