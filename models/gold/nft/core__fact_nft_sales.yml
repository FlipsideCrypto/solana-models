version: 2
models:
  - name: core__fact_nft_sales
    description: Deprecating soon! This is a notice that we're replacing this table with `nft__fact_nft_sales`. Please migrate queries to the new table by 9/14/23. There will be no column changes or any other changes of any kind. 
    columns:
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: BLOCK_ID
        description: "{{ doc('block_id') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: TX_ID
        description: "{{ doc('tx_id') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: SUCCEEDED
        description: "{{ doc('tx_succeeded') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: PROGRAM_ID 
        description: "{{ doc('program_id') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: PURCHASER
        description: "{{ doc('purchaser') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: SELLER
        description: "{{ doc('seller') }}"
        tests: 
          - dbt_expectations.expect_column_to_exist
      - name: MINT
        description: "{{ doc('mint') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: SALES_AMOUNT
        description: "{{ doc('sales_amount') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: MARKETPLACE
        description: "{{ doc('marketplace') }}"
        tests:
          - dbt_expectations.expect_column_to_exist