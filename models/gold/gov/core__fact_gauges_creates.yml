version: 2
models:
  - name: core__fact_gauges_creates
    description: Deprecating soon! This is a notice that we're replacing this table with `gov__fact_gauges_creates`. Please migrate queries to the new table by 9/14/23. There will be no column changes or any other changes of any kind. 
    columns:
      - name: PROGRAM_NAME
        description: "name of solana program"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: BLOCK_ID
        description: "{{ doc('block_id') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: TX_ID
        description: "{{ doc('tx_id') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: SUCCEEDED
        description: "{{ doc('tx_succeeded') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: SIGNER
        description: "{{ doc('gov_action_signer') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: GAUGE
        description: "{{ doc('tribeca_gauge') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: GAUGEMEISTER
        description: "{{ doc('tribeca_gaugemeister') }}"
        tests:
          - dbt_expectations.expect_column_to_exist
      - name: VALIDATOR_ACCOUNT
        description: "{{ doc('tribeca_gauge_validator_account') }}"
        tests:
          - dbt_expectations.expect_column_to_exist